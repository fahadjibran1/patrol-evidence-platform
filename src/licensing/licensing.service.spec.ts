/**
 * Snapshot mapping coverage lives alongside commercial-licensing.spec.ts.
 */
describe('LicensingService', () => {
  it('defers detailed coverage to commercial offline licensing tests', () => {
    expect(true).toBe(true);
  });
});
